# Travis build file for alphaTab

# Well, our language is Haxe but as I we use Ant for building
# we choose Java. 
language: java
  
before_install:
  # Install Haxe before build
  - sudo apt-get update                               
  - sudo apt-get install python-software-properties -y
  - sudo add-apt-repository ppa:eyecreate/haxe -y
  - sudo apt-get update
  - sudo apt-get install haxe -y --force-yes
  - mkdir ~/haxelib
  - haxelib setup ~/haxelib
  # Install NPM Packages 
  - npm install uglify-js -g
  # Work on head
  - git checkout master
  # Install HaxeLib Packages
  - haxelib install format
  - haxelib install mconsole

# Actual build
script:
  - ant flash
  
# Push back rebuilt sources to repository
env:
  global:
  secure: "jaaHnIPjBA07y94vQVo/D5Gih0p+U6ByWKDN4coSy1vSTPfHkhR1Ouf61xt59uxosgaldklg6qITcvUDATxvs67XhyDSkAuPHZH01gaSjAuXCdJEcx5pJuUM+oigCFmAhQGLMD4jeDfPq6sc7bCRA+hF+Tza3/XFwXH0XhQUlvM="
  
after_success: 
  - git config --global user.name "travis-ci" 
  - git config --global user.email "travis@travis-ci.org" 
  - git status 
  - git add -A 
  - git commit -m "Travis autobuild $TRAVIS_BUILD_NUMBER"
  - git push -q https://${GH_TOKEN}@github.com/CoderLine/alphaSynth master